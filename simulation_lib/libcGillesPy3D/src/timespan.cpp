#include "timespan.hpp"

GillesPy3D::Timespan::Timespan(int num_timesteps, double timestep_size, double output_freq)
: num_timesteps(num_timesteps), timestep_size(timestep_size), output_freq(output_freq)
{
}
